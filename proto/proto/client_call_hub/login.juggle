import common

enum em_platform {
  EPlatformGoogle = 1;
  EPlatformIphone = 2;
  EPlatformSteam = 3;
  EPlatformWXMiniGame = 4;
}

entity_service login {
    create_character req(string player_nick_name, em_role_gender gender, string appearance, string scene) rsp() err(int32 errCode);
    select_character req(string player_id) rsp() err(int32 errCode);
}