import common

entity_service player {
    into_scene req(string scene_name, int32 scene_line) rsp() err(int32 err_code);

    talk_npc req(int32 talk_id) rsp() err(int32 err_code);

    get_task_info ntf();
    completed_task ntf(int32 task_id);
}
