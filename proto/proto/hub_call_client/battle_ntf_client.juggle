import common

struct harm 
{
    string be_attack_entity_id;
    int32 skill_id;
    int32 harm_value;
    bool is_dead;
}

mutil_service battle_ntf_client {
    start_battle ntf(player_battle_info self_side, player_battle_info enemy);
    use_skill ntf(string caster, harm[] hits);
}