
# 嗯好很多了，有些中间的部分没办法抠除，可以在编辑功能里加一个超级橡皮，可以设置容差值，点击时可以删除一整块相近颜色的色块，快捷键是R，

=================================================

#现在有个很棘手的问题，抠图的问题，图片是白色背景，角色人物抠图时候，背景被抠了，里面的眼白，衣领也被抠除了，这怎么搞？？不能识别外外轮廓么？连身体内部的衣服也扣掉了，还有这两个：最小元素面积，外边距 Padding，是做什么的？我动了，好像也没什么变化。能否解决这个抠图问题

===========================

# 编辑图片的时候，在画笔被选中激活的状态下，鼠标要是一个圆圈的模样，调整大小的时候要随之改变。
# 编辑图片的时候，吸管是不能用的，吧吸管的快捷键设置为alt键，注意，这里是，在画笔被选中激活的状态，按死alt键时，变成吸色，鼠标的吸管图标出现，鼠标垫位置反正哪里，上面的样式就同步显示鼠标位置的颜色，点击鼠标左键时，确定颜色选择，并且变成画笔激活状态，

# 编辑功能里增加一个背景功能，默认现在是透明的，需要增加一个背景色来观察图片。

# 我编辑了素材里的图片，修改保存之后，在素材库里的图片显示没有随之修改，点击之后显示是修改了的。

# 在图集打包，我是不太明白，我选择了高宽，下面的预览跟我的选择的高宽完全不匹配，我选择的是1024x2048,选了四个角色图片，下面显示显示752 × 752· 2 帧，图集打包很重要，不能出错的，参考你可以看下：

================================================

# 工作台拖拽的文件被导入之后，中间的区域，无法放大缩小，鼠标移动等操作，参考： UI界面里，鼠标放大缩小，中键的平移。

# 工作台左 Alpha 边缘收缩向内腐蚀 1px，彻底去白边，像上面的颜色容差给我自由拖拉的滑块，自己设置收缩向内腐蚀多少px。

# 点击入库之后，就直接进入素材库了，为什么要在素材库左下搞一个工作太入库按钮？这个没必要。

# 素材库里是后台的话，自定义分类，分类里面可以再分类，批量移动和删除好像有问题，请参考：里面的分类和数据管理方式。

# 在素材库里的素材点击之后，会突然变大，然后鼠标放在其他素材上会有个弹窗不停闪烁显示，必须从新刷新网页。这样设计，做一个编辑功能，可以编辑素材，简单的画笔，画笔的大小调节，颜色选择，颜色吸取，有重做/撤销上一步，橡皮擦，调节功能：缩放旋转，左右反转等功能，点击某一个素材，立刻弹出编辑层，鼠标滚轮放大缩小，中键平移，点击调节功能按钮，素材就会出现句柄可，缩放旋转，左右反转，点击保存，就将修改保存到原图中，点击关闭按钮就退出编辑不保存，要有警告提示。

# 图集打包预览能不能弹出个相对大点的窗口，现在显示在右下角，好别扭。

=====================================

网站要改版，增加后台数据库的支持，生成的图片会记录到数据库里，数据库用sqlite.
- 后台图片要可以自定义分类，批量移动，增伤改查，
- 批量导入的图片,分割和抠图之后，可以导入数据库。

- 我可以直接从数据库拿出指定想要的PNG导出下载。

- 我可以直接从数据库拿出指定想要的PNG做出图集和plist文件。

- 要增加一个打包为图集，输出单张png图集和plist文件可以个给cocos直接使用。这个是我使用网站的批量抠图，然后导出给TexturePacker，它输出了单张png图集和plist文件在这个目录下，把我给它指定的png图片，一个一个排列好，并且输出成单张png图集，我希望你能做出他的功能，这些是技术参考：
- 
- 原来的抠图还是有一定的白边，我希望得到改善，不影响效率的情况下批量抠图更完美，这个是技术参考：

-  http://localhost:5173/ 我无法启动，吧端口改成5180，并且一个快捷启动给我，你要自己验证可以启动了才交付给我。